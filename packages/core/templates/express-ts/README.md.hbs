# {{projectName}}

Generated by Root — pure-engineering backend scaffolding.

## Stack

- Node.js + TypeScript
- Express (layered MVC)
- Database: **{{database}}**
- ORM/ODM: **{{orm}}**
{{#if testingVitest}}- Vitest{{/if}}
{{#if docker}}- Docker Compose{{/if}}

## Connection strings

{{#if isPostgres}}
```bash
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/{{projectName}}?schema=public
```
{{/if}}
{{#if isMysql}}
```bash
DATABASE_URL=mysql://root:root@localhost:3306/{{projectName}}
```
{{/if}}
{{#if isMongo}}
```bash
DATABASE_URL=mongodb://root:root@localhost:27017/{{projectName}}?authSource=admin
```
{{/if}}
{{#if isNoDb}}
No database selected. Add one later or re-scaffold with Postgres/MySQL/Mongo.
{{/if}}

## Quick start

```bash
cp .env.example .env
{{#if docker}}docker compose up -d{{/if}}
pnpm install
{{#if isPrisma}}pnpm prisma:generate{{/if}}
pnpm dev
```

Health check: [http://localhost:3000/health](http://localhost:3000/health)

{{#if isMongoose}}
> Mongo connection is lazy — call `connectDb()` from `src/db/client.ts` when you add data access.
{{/if}}
{{#if isNoOrm}}
> No ORM client was wired. `src/db/client.ts` is a stub until you add Prisma/Drizzle/Mongoose.
{{/if}}

## Root commands

```bash
pnpm dlx root@latest add auth
pnpm dlx root@latest add resource post
pnpm dlx root@latest doctor
```

{{#if authJwt}}
## Authentication

JWT auth is included. Sign up / sign in at \`/auth/signup\` and \`/auth/signin\`, then send:

\`\`\`bash
Authorization: Bearer <token>
\`\`\`

on mutating resource routes (POST/PUT/DELETE). Set \`ACCESS_TOKEN_SECRET\` in \`.env\`.
{{/if}}
