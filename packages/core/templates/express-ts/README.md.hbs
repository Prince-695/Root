# {{projectName}}

Generated by [Root](https://github.com/) — pure-engineering backend scaffolding.

## Stack

- Node.js + TypeScript
- Express (layered MVC)
- PostgreSQL + Prisma
{{#if testingVitest}}- Vitest{{/if}}
{{#if docker}}- Docker Compose (Postgres){{/if}}

## Quick start

```bash
cp .env.example .env
{{#if docker}}docker compose up -d{{/if}}
pnpm install
pnpm prisma:generate
pnpm dev
```

Health check: [http://localhost:3000/health](http://localhost:3000/health)

## Root commands

This project is Root-managed (`root.json`).

```bash
pnpm dlx root@latest add auth
pnpm dlx root@latest add route post
pnpm dlx root@latest doctor
```

## Scripts

| Script | Description |
|---|---|
| `pnpm dev` | Start API with hot reload |
| `pnpm build` | Compile TypeScript |
| `pnpm start` | Run compiled `dist/` |
| `pnpm test` | Run tests |
| `pnpm prisma:migrate` | Create/apply migrations |
