/**
 * No ORM selected. Add a data layer later with:
 *   npx rooot@latest add resource <name>
 * or re-init with Prisma / Drizzle / Mongoose.
 */
export const db = {
  async ping() {
    return { ok: true as const, mode: "none" as const };
  },
};
