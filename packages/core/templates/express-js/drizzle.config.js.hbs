import { defineConfig } from "drizzle-kit";

export default defineConfig({
  schema: "./src/db/schema.js",
  out: "./drizzle",
  dialect: "{{#if isPostgres}}postgresql{{/if}}{{#if isMysql}}mysql{{/if}}{{#if isSqlite}}sqlite{{/if}}",
  dbCredentials: {
    url: process.env.DATABASE_URL ?? "{{databaseUrlDefault}}",
  },
});
