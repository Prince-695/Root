generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "{{prismaProvider}}"
  url      = env("DATABASE_URL")
}

// Models are added by `pnpm dlx rootcli@latest add resource <name>`
